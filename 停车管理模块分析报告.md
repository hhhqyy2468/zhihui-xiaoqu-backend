# 停车管理模块 - 详细分析报告

> 模块状态：**开发中**
> 分析日期：2025-01-31
> 分析范围：数据库 + 后端 + 前端全栈代码

---

## 一、模块概述

停车管理模块是物业管理系统中的核心功能之一，负责管理小区车位资源、处理业主的车位租赁申请、维护车位租赁记录等业务。

### 1.1 核心功能

1. **车位管理**：车位信息的维护、状态管理
2. **租赁申请**：业主提交车位租赁申请
3. **审核流程**：管理员审核租赁申请
4. **租赁记录**：历史租赁记录查询（前端模拟数据）

---

## 二、数据库设计

### 2.1 车位表 (parking_space)

```sql
CREATE TABLE parking_space (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '车位ID',
    space_no VARCHAR(50) NOT NULL COMMENT '车位编号',
    location VARCHAR(200) NOT NULL COMMENT '车位位置（地下/地上，区域，层，区，号位）',
    space_status TINYINT NOT NULL DEFAULT 1 COMMENT '车位状态：1-空闲 2-已租 3-维修中',
    monthly_rent DECIMAL(10,2) DEFAULT 0.00 COMMENT '月租金',
    remark VARCHAR(500) COMMENT '备注',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记：0-未删除 1-已删除',
    create_by VARCHAR(64) DEFAULT '' COMMENT '创建人',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_by VARCHAR(64) DEFAULT '' COMMENT '更新人',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY uk_space_no (space_no),
    KEY idx_location (location),
    KEY idx_space_status (space_status),
    KEY idx_deleted (deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='车位表';
```

**字段说明**：
- `space_no`：车位编号，唯一索引
- `location`：车位位置描述
- `space_status`：车位状态（1-空闲，2-已租，3-维修中）
- `monthly_rent`：月租金
- 逻辑删除设计（deleted字段）

### 2.2 车位租赁申请表 (parking_rental_application)

```sql
CREATE TABLE parking_rental_application (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '申请ID',
    parking_space_id BIGINT NOT NULL COMMENT '车位ID',
    space_no VARCHAR(50) NOT NULL COMMENT '车位编号',
    owner_id BIGINT NOT NULL COMMENT '业主ID',
    owner_name VARCHAR(100) NOT NULL COMMENT '业主姓名',
    contact_phone VARCHAR(20) NOT NULL COMMENT '联系电话',
    vehicle_number VARCHAR(20) NOT NULL COMMENT '车辆号码',
    vehicle_brand VARCHAR(100) COMMENT '车辆品牌',
    vehicle_color VARCHAR(50) COMMENT '车辆颜色',
    rental_start_date DATE NOT NULL COMMENT '期望开始租赁日期',
    rental_end_date DATE COMMENT '期望结束租赁日期',
    rental_months INT DEFAULT 1 COMMENT '租赁月数',
    application_reason TEXT COMMENT '申请原因',
    application_status TINYINT DEFAULT 1 COMMENT '申请状态：1-待审核 2-已通过 3-已驳回',
    review_user_id BIGINT COMMENT '审核人ID',
    review_user_name VARCHAR(100) COMMENT '审核人姓名',
    review_time DATETIME COMMENT '审核时间',
    review_remark VARCHAR(500) COMMENT '审核备注',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记',
    create_by VARCHAR(64) DEFAULT '' COMMENT '创建人',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_by VARCHAR(64) DEFAULT '' COMMENT '更新人',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    KEY idx_parking_space_id (parking_space_id),
    KEY idx_owner_id (owner_id),
    KEY idx_application_status (application_status),
    KEY idx_deleted (deleted),
    KEY idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='车位租赁申请表';
```

**字段说明**：
- `parking_space_id`：关联车位表
- `vehicle_number`：车牌号
- `application_status`：申请状态流转（待审核→已通过/已驳回）
- 冗余字段设计：`space_no`, `owner_name` 减少关联查询

### 2.3 数据表关系

```
parking_space (1) ←───── (N) parking_rental_application
    ↑                           ↑
   车位                      租赁申请
    ↓                           ↓
  一个车位              可以有多个申请
```

---

## 三、后端架构

### 3.1 实体类设计

#### ParkingSpace.java
**文件路径**：`src/main/java/com/hyu/property/domain/ParkingSpace.java`

**核心字段**：
```java
@TableId(value = "id", type = IdType.AUTO)
private Long id;

private String spaceNo;           // 车位编号
private String location;          // 车位位置
private Integer spaceStatus;      // 车位状态
private BigDecimal monthlyRent;   // 月租金
private Integer deleted;          // 删除标记
```

**扩展字段（非数据库）**：
```java
@TableField(exist = false)
private String spaceStatusName;   // 状态名称
private Boolean isAvailable;      // 是否可用
private String currentTenant;     // 当前租户
private LocalDateTime rentalEndTime; // 租赁到期时间
private String vehicleInfo;       // 车辆信息
```

**设计亮点**：
- 使用 `@TableField(exist = false)` 标记非数据库字段
- 为前端提供友好的显示字段
- 状态名称通过 Service 层填充

#### ParkingRentalApplication.java
**文件路径**：`src/main/java/com/hyu/property/domain/ParkingRentalApplication.java`

**核心字段**：
```java
@TableId(type = IdType.AUTO)
private Long id;

private Long parkingSpaceId;      // 车位ID
private String spaceNo;            // 车位编号（冗余）
private Long ownerId;              // 业主ID
private String ownerName;          // 业主姓名（冗余）
private String contactPhone;       // 联系电话
private String vehicleNumber;      // 车辆号码
private Date rentalStartDate;      // 租赁开始日期
private Date rentalEndDate;        // 租赁结束日期
private Integer rentalMonths;      // 租赁月数
private Integer applicationStatus; // 申请状态
```

**审核相关字段**：
```java
private Long reviewUserId;         // 审核人ID
private String reviewUserName;     // 审核人姓名
private Date reviewTime;           // 审核时间
private String reviewRemark;       // 审核备注
```

**设计亮点**：
- 日期字段使用 `@JsonFormat` 统一格式
- 冗余车位编号和业主姓名，减少查询
- 完整的审核流程字段

### 3.2 Mapper层

#### ParkingSpaceMapper.java
```java
@Mapper
public interface ParkingSpaceMapper extends BaseMapper<ParkingSpace> {
    // 继承 MyBatis Plus 的 BaseMapper
    // 自动拥有 CRUD 方法
}
```

#### ParkingRentalApplicationMapper.java
```java
@Mapper
public interface ParkingRentalApplicationMapper extends BaseMapper<ParkingRentalApplication> {
    List<ParkingRentalApplication> selectParkingRentalApplicationList(
        ParkingRentalApplication parkingRentalApplication);

    ParkingRentalApplication selectParkingRentalApplicationById(@Param("id") Long id);

    List<ParkingRentalApplication> selectPendingApplications();
}
```

**设计说明**：
- 继承 `BaseMapper` 获得 MyBatis Plus 增强功能
- 自定义方法在 XML 中实现（未找到对应 XML 文件）
- `selectPendingApplications()` 查询待审核申请

### 3.3 Service层

#### IParkingSpaceService 接口
```java
public interface IParkingSpaceService extends IService<ParkingSpace> {
    Page<ParkingSpace> selectParkingSpacePage(Page<ParkingSpace> page, ParkingSpace parkingSpace);
    List<ParkingSpace> selectParkingSpaceList(ParkingSpace parkingSpace);
    ParkingSpace selectParkingSpaceById(Long id);
    boolean insertParkingSpace(ParkingSpace parkingSpace);
    boolean updateParkingSpace(ParkingSpace parkingSpace);
    boolean deleteParkingSpaceByIds(List<Long> ids);
    boolean deleteParkingSpaceById(Long id);
    Map<String, Object> getParkingSpaceStats();
}
```

#### ParkingSpaceServiceImpl 实现类
**文件路径**：`src/main/java/com/hyu/property/service/impl/ParkingSpaceServiceImpl.java`

**核心方法实现**：

1. **分页查询**
```java
@Override
public Page<ParkingSpace> selectParkingSpacePage(Page<ParkingSpace> page, ParkingSpace parkingSpace) {
    QueryWrapper<ParkingSpace> queryWrapper = new QueryWrapper<>();
    queryWrapper.eq("deleted", 0);

    if (StringUtils.isNotEmpty(parkingSpace.getSpaceNo())) {
        queryWrapper.like("space_no", parkingSpace.getSpaceNo());
    }
    if (StringUtils.isNotEmpty(parkingSpace.getLocation())) {
        queryWrapper.like("location", parkingSpace.getLocation());
    }
    if (parkingSpace.getSpaceStatus() != null) {
        queryWrapper.eq("space_status", parkingSpace.getSpaceStatus());
    }

    queryWrapper.orderByDesc("create_time");
    return this.page(page, queryWrapper);
}
```

2. **统计数据**（亮点功能）
```java
@Override
public Map<String, Object> getParkingSpaceStats() {
    Map<String, Object> stats = new HashMap<>();

    // 总车位数
    long totalCount = this.count(queryWrapper);
    stats.put("totalCount", totalCount);

    // 空闲车位数
    queryWrapper.eq("space_status", 1);
    long availableCount = this.count(queryWrapper);
    stats.put("availableCount", availableCount);

    // 已租车位数
    queryWrapper.clear();
    queryWrapper.eq("deleted", 0);
    queryWrapper.eq("space_status", 2);
    long rentedCount = this.count(queryWrapper);
    stats.put("rentedCount", rentedCount);

    // 维修中车位数
    queryWrapper.clear();
    queryWrapper.eq("deleted", 0);
    queryWrapper.eq("space_status", 3);
    long maintenanceCount = this.count(queryWrapper);
    stats.put("maintenanceCount", maintenanceCount);

    // 使用率
    double usageRate = totalCount > 0 ? (double) rentedCount / totalCount * 100 : 0;
    stats.put("usageRate", Math.round(usageRate * 100.0) / 100.0);

    return stats;
}
```

**返回数据示例**：
```json
{
    "totalCount": 500,
    "availableCount": 200,
    "rentedCount": 280,
    "maintenanceCount": 20,
    "usageRate": 56.0
}
```

3. **状态名称填充**
```java
private void setSpaceStatusName(ParkingSpace parkingSpace) {
    if (parkingSpace.getSpaceStatus() != null) {
        switch (parkingSpace.getSpaceStatus()) {
            case 1: parkingSpace.setSpaceStatusName("空闲"); break;
            case 2: parkingSpace.setSpaceStatusName("已租"); break;
            case 3: parkingSpace.setSpaceStatusName("维修中"); break;
            default: parkingSpace.setSpaceStatusName("未知"); break;
        }
    }
}
```

#### IParkingRentalApplicationService 接口
```java
public interface IParkingRentalApplicationService {
    ParkingRentalApplication selectParkingRentalApplicationById(Long id);
    List<ParkingRentalApplication> selectParkingRentalApplicationList(...);
    int insertParkingRentalApplication(ParkingRentalApplication parkingRentalApplication);
    int updateParkingRentalApplication(ParkingRentalApplication parkingRentalApplication);
    int deleteParkingRentalApplicationByIds(Long[] ids);
    Page<ParkingRentalApplication> selectApplicationPage(...);
    List<ParkingRentalApplication> selectPendingApplications();
    boolean reviewApplication(Long id, Integer status, String reviewRemark,
                             Long reviewUserId, String reviewUserName);
}
```

#### ParkingRentalApplicationServiceImpl 实现类
**文件路径**：`src/main/java/com/hyu/property/service/impl/ParkingRentalApplicationServiceImpl.java`

**核心方法：审核申请**
```java
@Override
public boolean reviewApplication(Long id, Integer status, String reviewRemark,
                                Long reviewUserId, String reviewUserName) {
    ParkingRentalApplication application = new ParkingRentalApplication();
    application.setId(id);
    application.setApplicationStatus(status);
    application.setReviewRemark(reviewRemark);
    application.setReviewUserId(reviewUserId);
    application.setReviewUserName(reviewUserName);
    application.setReviewTime(new Date());

    return baseMapper.updateById(application) > 0;
}
```

**新增申请**：
```java
@Override
public int insertParkingRentalApplication(ParkingRentalApplication parkingRentalApplication) {
    parkingRentalApplication.setApplicationStatus(1); // 默认待审核
    parkingRentalApplication.setDeleted(0);
    return baseMapper.insert(parkingRentalApplication);
}
```

### 3.4 Controller层

#### ParkingSpaceController
**文件路径**：`src/main/java/com/hyu/property/controller/ParkingSpaceController.java`

**基础路径**：`/api/v1/parking/space`

**API接口清单**：

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /list | 分页查询车位列表 | parking:space:list |
| GET | /{id} | 获取车位详情 | parking:space:list |
| POST | / | 新增车位 | parking:space:add |
| PUT | / | 修改车位 | parking:space:edit |
| DELETE | /{ids} | 删除车位 | parking:space:delete |
| GET | /statistics | 获取车位统计 | parking:space:list |

**代码示例**：
```java
@GetMapping("/list")
@PreAuthorize("@ss.hasPermi('parking:space:list')")
public AjaxResult list(@RequestParam(defaultValue = "1") Integer pageNum,
                      @RequestParam(defaultValue = "10") Integer pageSize,
                      @RequestParam(required = false) String spaceNo,
                      @RequestParam(required = false) String location,
                      @RequestParam(required = false) Integer status) {
    Page<ParkingSpace> page = new Page<>(pageNum, pageSize);
    ParkingSpace parkingSpace = new ParkingSpace();
    parkingSpace.setSpaceNo(spaceNo);
    parkingSpace.setLocation(location);
    parkingSpace.setSpaceStatus(status);

    Page<ParkingSpace> result = parkingSpaceService.selectParkingSpacePage(page, parkingSpace);
    return AjaxResult.success("查询成功",
        PageResult.success(result.getTotal(), result.getRecords()));
}
```

**字段映射处理**（亮点）：
```java
private void addFrontendFields(ParkingSpace parkingSpace) {
    // 前端期望 status 字段，后端是 spaceStatus
    if (parkingSpace.getSpaceStatus() != null) {
        parkingSpace.setSpaceStatusName(getStatusName(parkingSpace.getSpaceStatus()));
    }
    parkingSpace.setCurrentTenant("");
    parkingSpace.setIsAvailable(parkingSpace.getSpaceStatus() != null
        && parkingSpace.getSpaceStatus() == 1);
}
```

#### ParkingRentalApplicationController
**文件路径**：`src/main/java/com/hyu/property/controller/ParkingRentalApplicationController.java`

**基础路径**：`/api/v1/parking/rental/application`

**API接口清单**：

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /list | 查询租赁申请列表 | 无 |
| GET | /{id} | 获取申请详情 | 无 |
| POST | / | 新增租赁申请 | 无 |
| PUT | / | 修改租赁申请 | 无 |
| DELETE | /{ids} | 删除租赁申请 | 无 |
| PUT | /{id}/review | 审核租赁申请 | 无 |
| GET | /pending | 获取待审核申请列表 | 无 |

**审核接口实现**：
```java
@PutMapping("/{id}/review")
public AjaxResult review(@PathVariable("id") Long id,
                      @RequestParam Integer status,
                      @RequestParam(required = false) String reviewRemark) {
    boolean result = parkingRentalApplicationService.reviewApplication(
        id, status, reviewRemark, 1L, "admin"); // 硬编码审核人信息

    return result ? AjaxResult.success() : AjaxResult.error();
}
```

**问题分析**：
- ❌ 审核人信息硬编码，应该从 `SecurityUtils` 获取当前登录用户
- ❌ 缺少权限注解 `@PreAuthorize`

**简单分页处理**（问题）：
```java
@GetMapping("/list")
public AjaxResult list(...) {
    List<ParkingRentalApplication> applications =
        parkingRentalApplicationService.selectParkingRentalApplicationList(...);

    // 简单分页处理（在内存中分页，性能较差）
    int total = applications.size();
    int start = (pageNum - 1) * pageSize;
    int end = Math.min(start + pageSize, total);
    List<ParkingRentalApplication> pageData =
        applications.subList(start < total ? start : total, end);

    return AjaxResult.success("查询成功", PageResult.success((long) total, pageData));
}
```

**性能问题**：
- ❌ 先查询所有数据，再在内存中分页
- ❌ 数据量大时会有严重的性能问题
- ✅ 应该使用 Service 层的 `selectApplicationPage` 方法

---

## 四、前端架构

### 4.1 API封装

#### parkingSpace.js
**文件路径**：`src/api/parkingSpace.js`

```javascript
import request from '@/utils/request'

// 获取车位列表
export function listParkingSpaces(params) {
  return request({
    url: '/parking/space/list',
    method: 'get',
    params
  })
}

// 新增车位
export function addParkingSpace(data) {
  return request({
    url: '/parking/space',
    method: 'post',
    data
  })
}

// 批量删除车位
export function deleteParkingSpaces(spaceIds) {
  return request({
    url: `/parking/space/${spaceIds.join(',')}`,
    method: 'delete'
  })
}

// 获取车位统计数据
export function getParkingSpaceStats() {
  return request({
    url: '/parking/space/statistics',
    method: 'get'
  })
}
```

**设计说明**：
- 统一的请求封装
- RESTful API 风格
- 批量删除使用逗号分隔的 ID

#### rentalApplication.js
**文件路径**：`src/api/parking/rentalApplication.js`

```javascript
// 审核租赁申请
export function reviewApplication(id, status, reviewRemark) {
  return request({
    url: '/parking/rental/application/' + id + '/review',
    method: 'put',
    params: {
      status: status,
      reviewRemark: reviewRemark
    }
  })
}

// 查询待审核申请列表
export function getPendingApplications() {
  return request({
    url: '/parking/rental/application/pending',
    method: 'get'
  })
}
```

### 4.2 页面组件

#### 车位管理页面
**文件路径**：`src/views/parking/space/index.vue`

**页面结构**：
```vue
<template>
  <div class="log-container">
    <!-- 页面标题 + 面包屑 -->
    <div class="page-header">...</div>

    <!-- 标签页 -->
    <el-tabs v-model="activeTab">
      <el-tab-pane label="车位管理" name="list">
        <!-- 搜索区域 -->
        <div class="search-section">
          <el-form inline>
            <el-input v-model="searchForm.spaceNo" />
            <el-input v-model="searchForm.location" />
            <el-select v-model="searchForm.status">
              <el-option label="空闲" :value="1" />
              <el-option label="已租" :value="2" />
              <el-option label="维修中" :value="3" />
            </el-select>
          </el-form>
        </div>

        <!-- 操作按钮 -->
        <div class="action-section">
          <el-button @click="handleAdd">新增车位</el-button>
          <el-button @click="handleBatchDelete">批量删除</el-button>
        </div>

        <!-- 车位表格 -->
        <el-table :data="parkingSpaceList">
          <el-table-column type="selection" />
          <el-table-column prop="spaceNo" label="车位编号" />
          <el-table-column prop="location" label="车位位置" />
          <el-table-column prop="spaceStatus" label="车位状态">
            <template #default="{ row }">
              <el-tag :type="getStatusColor(row.spaceStatus)">
                {{ getStatusName(row.spaceStatus) }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="monthlyRent" label="月租金">
            <template #default="{ row }">
              ¥{{ row.monthlyRent }}
            </template>
          </el-table-column>
          <el-table-column label="操作">
            <el-button link @click="handleEdit(row)">编辑</el-button>
            <el-button link @click="handleViewRentals(row)">租赁记录</el-button>
            <el-button link @click="handleDelete(row)">删除</el-button>
          </el-table-column>
        </el-table>

        <!-- 分页 -->
        <el-pagination />
      </el-tab-pane>
    </el-tabs>

    <!-- 新增/编辑对话框 -->
    <el-dialog v-model="dialogVisible">
      <el-form :model="form">
        <el-form-item label="车位编号" prop="spaceNo">
          <el-input v-model="form.spaceNo" />
        </el-form-item>
        <el-form-item label="车位位置" prop="location">
          <el-input v-model="form.location" />
        </el-form-item>
        <el-form-item label="月租金" prop="monthlyRent">
          <el-input-number v-model="form.monthlyRent" :min="0" :precision="2" />
        </el-form-item>
        <el-form-item label="车位状态" prop="spaceStatus">
          <el-radio-group v-model="form.spaceStatus">
            <el-radio :value="1">空闲</el-radio>
            <el-radio :value="2">已租</el-radio>
            <el-radio :value="3">维修中</el-radio>
          </el-radio-group>
        </el-form-item>
      </el-form>
    </el-dialog>
  </div>
</template>
```

**状态颜色映射**：
```javascript
const getStatusColor = (status) => {
  const colorMap = {
    1: 'success',  // 空闲 - 绿色
    2: 'warning',  // 已租 - 橙色
    3: 'danger'    // 维修中 - 红色
  }
  return colorMap[status] || 'info'
}
```

**表单验证**：
```javascript
const rules = {
  spaceNo: [{ required: true, message: '请输入车位编号', trigger: 'blur' }],
  location: [{ required: true, message: '请输入车位位置', trigger: 'blur' }],
  spaceStatus: [{ required: true, message: '请选择车位状态', trigger: 'change' }],
  monthlyRent: [{ required: true, message: '请输入月租金', trigger: 'blur' }]
}
```

#### 租赁管理页面
**文件路径**：`src/views/parking/rental/index.vue`

**页面结构**：
```vue
<template>
  <el-tabs v-model="activeTab">
    <!-- 租赁申请 -->
    <el-tab-pane label="租赁申请" name="applications">
      <!-- 搜索 + 操作 + 表格 -->
      <el-button @click="handleBatchApprove">批量通过</el-button>
      <el-button @click="handleBatchReject">批量拒绝</el-button>

      <el-table :data="applicationList">
        <el-table-column type="selection" />
        <el-table-column prop="applicationNo" label="申请编号" />
        <el-table-column label="车位信息">
          {{ row.vehicleNumber }} - {{ row.spaceNo }}
        </el-table-column>
        <el-table-column prop="ownerName" label="申请人" />
        <el-table-column prop="applicationStatus" label="申请状态">
          <el-tag :type="getStatusColor(row.applicationStatus)">
            {{ getStatusName(row.applicationStatus) }}
          </el-tag>
        </el-table-column>
        <el-table-column label="操作">
          <el-button v-if="row.applicationStatus === 1" @click="handleApprove(row)">
            通过
          </el-button>
          <el-button v-if="row.applicationStatus === 1" @click="handleReject(row)">
            拒绝
          </el-button>
          <el-button @click="handleView(row)">查看</el-button>
        </el-table-column>
      </el-table>
    </el-tab-pane>

    <!-- 租赁记录 -->
    <el-tab-pane label="租赁记录" name="records">
      <!-- 使用模拟数据 -->
      <el-table :data="recordList">
        <el-table-column prop="contractNo" label="合同编号" />
        <el-table-column prop="tenant" label="承租人" />
        <el-table-column prop="spaceNo" label="车位编号" />
        <el-table-column prop="leaseStatus" label="租赁状态" />
        <el-table-column label="操作">
          <el-button @click="handleViewContract(row)">查看合同</el-button>
          <el-button v-if="row.leaseStatus === '1'" @click="handleTerminate(row)">
            终止
          </el-button>
        </el-table-column>
      </el-table>
    </el-tab-pane>
  </el-tabs>

  <!-- 审核对话框 -->
  <el-dialog title="租赁申请审核">
    <el-form>
      <el-form-item label="审核结果">
        <el-radio-group v-model="reviewForm.status">
          <el-radio :value="2">通过</el-radio>
          <el-radio :value="3">驳回</el-radio>
        </el-radio-group>
      </el-form-item>
      <el-form-item label="审核备注">
        <el-input type="textarea" v-model="reviewForm.reviewRemark" />
      </el-form-item>
    </el-form>
  </el-dialog>

  <!-- 申请详情对话框 -->
  <el-dialog title="申请详情">
    <el-descriptions :column="2" border>
      <el-descriptions-item label="车位编号">{{ currentApplication.spaceNo }}</el-descriptions-item>
      <el-descriptions-item label="业主姓名">{{ currentApplication.ownerName }}</el-descriptions-item>
      <el-descriptions-item label="车辆号码">{{ currentApplication.vehicleNumber }}</el-descriptions-item>
      <!-- ... 更多字段 -->
    </el-descriptions>
  </el-dialog>
</template>
```

**批量审核功能**：
```javascript
const handleBatchApprove = () => {
  if (selectedApplications.value.length === 0) {
    ElMessage.warning('请选择要审核的申请')
    return
  }

  ElMessageBox.confirm(
    `确定要通过选中的 ${selectedApplications.value.length} 个申请吗？`,
    '批量通过',
    { type: 'warning' }
  ).then(() => {
    selectedApplications.value.forEach(app => {
      if (app.applicationStatus === 1) {
        reviewApplication(app.id, 2, '批量审核通过').catch(() => {})
      }
    })
    ElMessage.success('批量审核成功')
    getList()
  })
}
```

**模拟数据生成**（待开发）：
```javascript
const generateMockRecords = () => {
  const records = []
  for (let i = 1; i <= 50; i++) {
    records.push({
      id: i,
      contractNo: `C${String(i).padStart(6, '0')}`,
      tenant: tenants[Math.floor(Math.random() * tenants.length)],
      spaceNo: `P${String(Math.floor(Math.random() * 500) + 1).padStart(3, '0')}`,
      monthlyRent: Math.floor(Math.random() * 500) + 200,
      leaseStatus: statuses[Math.floor(Math.random() * statuses.length)],
      // ...
    })
  }
  return records
}
```

---

## 五、业务流程分析

### 5.1 车位租赁完整流程

```
┌─────────────────────────────────────────────────────────────┐
│                      车位租赁流程                             │
└─────────────────────────────────────────────────────────────┘

【业主端】
1. 业主查看可用车位
   ↓
2. 提交租赁申请
   - 填写车辆信息（车牌、品牌、颜色）
   - 选择租赁期限（开始日期、月数）
   - 填写申请原因
   ↓
3. 系统创建申请记录（状态：待审核）

【物业管理端】
4. 管理员查看待审核申请
   ↓
5. 审核申请
   ├─ 通过 → 状态改为"已通过"
   │         - 需要更新车位状态为"已租"
   │         - 创建租赁合同（待实现）
   │         - 发送通知给业主
   └─ 驳回 → 状态改为"已驳回"
             - 填写驳回原因
             - 发送通知给业主

【后续】
6. 租赁到期后自动释放车位（定时任务，待实现）
7. 业主可以续租或退租（待实现）
```

### 5.2 状态流转图

```
车位状态流转：
┌──────┐
│ 空闲 │ ←──────────────┐
└──┬───┘               │
   │ 审核通过          │ 租赁到期
   ↓                   │
┌──────┐               │
│ 已租 │ ───────────────┘
└──┬───┘
   │ 维修
   ↓
┌──────┐
│维修中 │ ←─────────────┐
└──┬───┘              │
   │ 维修完成          │
   └───────────────────┘

申请状态流转：
┌──────────┐
│ 待审核   │
└────┬─────┘
     │
     ├─────────┐
     ↓         ↓
┌────────┐ ┌────────┐
│ 已通过 │ │ 已驳回 │
└────────┘ └────────┘
```

---

## 六、待开发功能清单

### 6.1 数据库层面

#### 1. 租赁合同表（缺失）
```sql
-- 建议新增
CREATE TABLE parking_rental_contract (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '合同ID',
    contract_no VARCHAR(50) NOT NULL UNIQUE COMMENT '合同编号',
    application_id BIGINT NOT NULL COMMENT '申请ID',
    parking_space_id BIGINT NOT NULL COMMENT '车位ID',
    space_no VARCHAR(50) NOT NULL COMMENT '车位编号',
    owner_id BIGINT NOT NULL COMMENT '业主ID',
    owner_name VARCHAR(100) NOT NULL COMMENT '业主姓名',
    vehicle_number VARCHAR(20) NOT NULL COMMENT '车辆号码',
    monthly_rent DECIMAL(10,2) NOT NULL COMMENT '月租金',
    rental_months INT NOT NULL COMMENT '租赁月数',
    start_date DATE NOT NULL COMMENT '租赁开始日期',
    end_date DATE NOT NULL COMMENT '租赁结束日期',
    total_amount DECIMAL(10,2) NOT NULL COMMENT '总金额',
    contract_status TINYINT DEFAULT 1 COMMENT '合同状态：1-进行中 2-已到期 3-已终止',
    sign_date DATE COMMENT '签订日期',
    terminate_date DATE COMMENT '终止日期',
    terminate_reason VARCHAR(500) COMMENT '终止原因',
    remark VARCHAR(500) COMMENT '备注',
    deleted TINYINT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_contract_no (contract_no),
    KEY idx_application_id (application_id),
    KEY idx_parking_space_id (parking_space_id),
    KEY idx_owner_id (owner_id),
    KEY idx_contract_status (contract_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='车位租赁合同表';
```

#### 2. 车位使用记录表（建议新增）
```sql
CREATE TABLE parking_space_usage_log (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    parking_space_id BIGINT NOT NULL COMMENT '车位ID',
    owner_id BIGINT COMMENT '业主ID',
    vehicle_number VARCHAR(20) COMMENT '车辆号码',
    action_type TINYINT NOT NULL COMMENT '操作类型：1-入位 2-出位 3-状态变更',
    old_status TINYINT COMMENT '原状态',
    new_status TINYINT COMMENT '新状态',
    remark VARCHAR(500) COMMENT '备注',
    operator_id BIGINT COMMENT '操作人ID',
    operator_name VARCHAR(100) COMMENT '操作人姓名',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_parking_space_id (parking_space_id),
    KEY idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='车位使用记录表';
```

### 6.2 后端层面

#### 1. Mapper XML 文件（缺失）
- ❌ 没有找到 `ParkingRentalApplicationMapper.xml`
- ✅ 需要创建自定义 SQL 查询的 XML 文件
- 📝 建议路径：`src/main/resources/mapper/property/ParkingRentalApplicationMapper.xml`

**建议内容**：
```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyu.property.mapper.ParkingRentalApplicationMapper">

    <select id="selectParkingRentalApplicationList" resultType="ParkingRentalApplication">
        SELECT
            a.*,
            s.location,
            s.monthly_rent
        FROM parking_rental_application a
        LEFT JOIN parking_space s ON a.parking_space_id = s.id
        WHERE a.deleted = 0
        <if test="spaceNo != null and spaceNo != ''">
            AND a.space_no LIKE CONCAT('%', #{spaceNo}, '%')
        </if>
        <if test="ownerName != null and ownerName != ''">
            AND a.owner_name LIKE CONCAT('%', #{ownerName}, '%')
        </if>
        <if test="vehicleNumber != null and vehicleNumber != ''">
            AND a.vehicle_number LIKE CONCAT('%', #{vehicleNumber}, '%')
        </if>
        <if test="applicationStatus != null">
            AND a.application_status = #{applicationStatus}
        </if>
        ORDER BY a.create_time DESC
    </select>

    <select id="selectPendingApplications" resultType="ParkingRentalApplication">
        SELECT
            a.*,
            s.location,
            s.monthly_rent
        FROM parking_rental_application a
        LEFT JOIN parking_space s ON a.parking_space_id = s.id
        WHERE a.deleted = 0
          AND a.application_status = 1
        ORDER BY a.create_time ASC
    </select>

</mapper>
```

#### 2. 审核逻辑优化（必要）

**当前问题**：
```java
// ParkingRentalApplicationController.java 第99行
boolean result = parkingRentalApplicationService.reviewApplication(
    id, status, reviewRemark, 1L, "admin"); // ❌ 硬编码
```

**建议修改**：
```java
@PutMapping("/{id}/review")
public AjaxResult review(@PathVariable("id") Long id,
                      @RequestParam Integer status,
                      @RequestParam(required = false) String reviewRemark) {
    log.info("审核租赁申请, id: {}, status: {}, reviewRemark: {}", id, status, reviewRemark);

    // ✅ 从 SecurityContext 获取当前用户
    Long currentUserId = SecurityUtils.getUserId();
    String currentUserName = SecurityUtils.getUsername();

    boolean result = parkingRentalApplicationService.reviewApplication(
        id, status, reviewRemark, currentUserId, currentUserName);

    // ✅ 审核通过时，更新车位状态
    if (result && status == 2) {
        ParkingRentalApplication application =
            parkingRentalApplicationService.selectParkingRentalApplicationById(id);
        if (application != null) {
            ParkingSpace parkingSpace = new ParkingSpace();
            parkingSpace.setId(application.getParkingSpaceId());
            parkingSpace.setSpaceStatus(2); // 已租
            parkingSpaceService.updateById(parkingSpace);
        }
    }

    return result ? AjaxResult.success() : AjaxResult.error();
}
```

#### 3. 租赁合同 Service（新增）
```java
public interface IParkingRentalContractService extends IService<ParkingRentalContract> {
    /**
     * 根据申请生成租赁合同
     */
    ParkingRentalContract generateContract(Long applicationId);

    /**
     * 终止合同
     */
    boolean terminateContract(Long contractId, String terminateReason);

    /**
     * 查询即将到期的合同
     */
    List<ParkingRentalContract> getExpiringContracts(int days);
}
```

#### 4. 定时任务（新增）
```java
@Component
public class ParkingRentTask {
    /**
     * 每天检查即将到期的租赁合同（提前7天提醒）
     */
    @Scheduled(cron = "0 0 9 * * ?") // 每天9点执行
    public void checkExpiringContracts() {
        // 查询7天内到期的合同
        // 发送提醒通知
    }

    /**
     * 每天检查已到期的合同并自动释放车位
     */
    @Scheduled(cron = "0 0 0 * * ?") // 每天0点执行
    public void releaseExpiredParkingSpaces() {
        // 查询已到期的合同
        // 更新车位状态为空闲
        // 更新合同状态为已到期
    }
}
```

#### 5. 权限注解添加（必要）
```java
// ParkingRentalApplicationController 缺少权限注解
@PreAuthorize("@ss.hasPermi('parking:rental:list')")
public AjaxResult list(...)

@PreAuthorize("@ss.hasPermi('parking:rental:review')")
public AjaxResult review(...)
```

### 6.3 前端层面

#### 1. 租赁记录功能（待实现）

**当前状态**：
- ❌ 使用模拟数据 `generateMockRecords()`
- ❌ 没有真实的后端 API

**需要实现**：
1. 创建租赁合同 API
2. 创建租赁合同页面组件
3. 实现合同查看功能（PDF预览）
4. 实现合同终止功能
5. 实现续租功能

**建议 API**：
```javascript
// src/api/parking/rentalContract.js
export function listContract(query) {
  return request({
    url: '/parking/rental/contract/list',
    method: 'get',
    params: query
  })
}

export function getContract(id) {
  return request({
    url: '/parking/rental/contract/' + id,
    method: 'get'
  })
}

export function terminateContract(id, terminateReason) {
  return request({
    url: '/parking/rental/contract/' + id + '/terminate',
    method: 'put',
    data: { terminateReason }
  })
}
```

#### 2. 车位可视化（建议新增）

**功能描述**：
- 车位平面图展示
- 车位状态颜色标识
- 点击查看详情
- 拖拽分配车位

**技术方案**：
```vue
<template>
  <div class="parking-lot">
    <!-- 车位网格 -->
    <div v-for="floor in floors" :key="floor.id" class="floor">
      <h3>{{ floor.name }}</h3>
      <div class="parking-grid">
        <div v-for="space in floor.spaces"
             :key="space.id"
             class="parking-space"
             :class="getSpaceClass(space)"
             @click="handleSpaceClick(space)">
          <div class="space-number">{{ space.spaceNo }}</div>
          <div class="space-status">{{ space.spaceStatusName }}</div>
          <div v-if="space.vehicleNumber" class="vehicle-number">
            {{ space.vehicleNumber }}
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
const getSpaceClass = (space) => {
  return {
    'space-available': space.spaceStatus === 1,
    'space-rented': space.spaceStatus === 2,
    'space-maintenance': space.spaceStatus === 3
  }
}
</script>

<style lang="scss" scoped>
.parking-space {
  width: 80px;
  height: 120px;
  border: 2px solid #ddd;
  border-radius: 4px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s;

  &.space-available {
    background-color: #67c23a;
    border-color: #67c23a;
  }

  &.space-rented {
    background-color: #e6a23c;
    border-color: #e6a23c;
  }

  &.space-maintenance {
    background-color: #f56c6c;
    border-color: #f56c6c;
  }

  &:hover {
    transform: scale(1.05);
    box-shadow: 0 2px 12px rgba(0,0,0,0.2);
  }
}
</style>
```

#### 3. 租赁申请优化

**当前问题**：
- ❌ 申请时没有选择车位的功能
- ❌ 直接输入车位编号，不友好

**建议改进**：
```vue
<el-form-item label="选择车位" prop="parkingSpaceId">
  <el-select
    v-model="form.parkingSpaceId"
    placeholder="请选择车位"
    filterable
    @change="handleSpaceChange">
    <el-option
      v-for="space in availableSpaces"
      :key="space.id"
      :label="`${space.spaceNo} - ${space.location}`"
      :value="space.id">
      <span>{{ space.spaceNo }}</span>
      <span style="float: right; color: #8492a6;">
        {{ space.location }} - ¥{{ space.monthlyRent }}/月
      </span>
    </el-option>
  </el-select>
</el-form-item>

<script setup>
// 加载可用车位
const loadAvailableSpaces = async () => {
  const response = await listParkingSpaces({
    spaceStatus: 1 // 只加载空闲车位
  })
  availableSpaces.value = response.data.rows
}

// 车位变化时自动填充信息
const handleSpaceChange = (spaceId) => {
  const space = availableSpaces.value.find(s => s.id === spaceId)
  if (space) {
    form.value.spaceNo = space.spaceNo
    form.value.monthlyRent = space.monthlyRent
  }
}
</script>
```

#### 4. 费用计算（建议新增）

**功能描述**：
- 自动计算租赁总金额
- 显示费用明细
- 支持折扣设置

```vue
<template>
  <div class="fee-calculator">
    <el-descriptions title="费用明细" :column="2" border>
      <el-descriptions-item label="月租金">
        ¥{{ form.monthlyRent }}
      </el-descriptions-item>
      <el-descriptions-item label="租赁月数">
        {{ form.rentalMonths }} 个月
      </el-descriptions-item>
      <el-descriptions-item label="小计">
        ¥{{ subtotal }}
      </el-descriptions-item>
      <el-descriptions-item label="折扣">
        <el-input-number
          v-model="form.discount"
          :min="0"
          :max="100"
          :precision="2"
          size="small" />
        元
      </el-descriptions-item>
      <el-descriptions-item label="总金额">
        <span class="total-amount">¥{{ totalAmount }}</span>
      </el-descriptions-item>
    </el-descriptions>
  </div>
</template>

<script setup>
const subtotal = computed(() => {
  return (form.value.monthlyRent || 0) * (form.value.rentalMonths || 1)
})

const totalAmount = computed(() => {
  return Math.max(0, subtotal.value - (form.value.discount || 0))
})
</script>
```

### 6.4 集成层面

#### 1. 与账单系统集成（必要）

**当前状态**：
- ❌ 租赁审核通过后没有生成账单

**建议实现**：
```java
@Transactional
public boolean reviewApplication(Long id, Integer status, String reviewRemark,
                                Long reviewUserId, String reviewUserName) {
    // 1. 更新申请状态
    // ...

    // 2. 审核通过时生成账单
    if (status == 2) {
        ParkingRentalApplication application = getById(id);

        // 生成租赁账单
        Bill bill = new Bill();
        bill.setBillNo(generateBillNo());
        bill.setUserId(application.getOwnerId());
        bill.setHouseId(getDefaultHouseId(application.getOwnerId())); // 获取业主房产

        // 获取车位费类型
        FeeType parkingFeeType = feeTypeService.getByTypeCode("PARKING_RENTAL_FEE");
        bill.setFeeTypeId(parkingFeeType.getId());
        bill.setFeeTypeName(parkingFeeType.getTypeName());

        // 计算费用
        BigDecimal totalAmount = application.getMonthlyRent()
            .multiply(new BigDecimal(application.getRentalMonths()));
        bill.setAmount(totalAmount);
        bill.setPaidAmount(BigDecimal.ZERO);
        bill.setBillStatus(1); // 待缴费
        bill.setBillPeriod(LocalDate.now().toString());
        bill.setDueDate(calculateDueDate(application.getRentalStartDate()));
        bill.setRemark("车位租赁费 - " + application.getSpaceNo());

        billService.insertBill(bill);

        // 生成租赁合同
        generateContract(application, bill);
    }

    return true;
}
```

#### 2. 与通知系统集成（建议）

**功能描述**：
- 申请提交成功通知
- 审核结果通知
- 即将到期提醒
- 已到期通知

```java
// 审核通过后发送通知
if (status == 2) {
    Notification notification = new Notification();
    notification.setUserId(application.getOwnerId());
    notification.setTitle("车位租赁申请审核通过");
    notification.setContent("您申请的车位 " + application.getSpaceNo() + " 已审核通过，请及时缴纳费用。");
    notification.setType("RENTAL_APPROVED");
    notificationService.send(notification);
}
```

---

## 七、问题总结与建议

### 7.1 当前存在的问题

#### 严重问题（必须修复）

1. **审核人信息硬编码**
   - 位置：`ParkingRentalApplicationController.java:99`
   - 问题：`reviewApplication(..., 1L, "admin")` 硬编码
   - 影响：无法追踪真实审核人
   - 修复：使用 `SecurityUtils.getUserId()` 和 `getUsername()`

2. **分页性能问题**
   - 位置：`ParkingRentalApplicationController.java:44-48`
   - 问题：在内存中分页，数据量大时性能差
   - 影响：系统响应慢，内存占用高
   - 修复：使用 Service 层的 `selectApplicationPage` 方法

3. **缺少 Mapper XML**
   - 位置：`src/main/resources/mapper/`
   - 问题：自定义 SQL 没有实现
   - 影响：查询功能可能无法正常工作
   - 修复：创建对应的 XML 文件

#### 中等问题（建议修复）

4. **缺少权限注解**
   - 位置：`ParkingRentalApplicationController`
   - 问题：所有接口都没有 `@PreAuthorize` 注解
   - 影响：权限控制不严格
   - 修复：添加相应的权限注解

5. **审核通过后未更新车位状态**
   - 位置：`ParkingRentalApplicationServiceImpl`
   - 问题：审核通过后车位状态仍为空闲
   - 影响：可能导致一个车位被多次租赁
   - 修复：审核通过时同步更新车位状态

6. **没有生成租赁账单**
   - 位置：审核流程
   - 问题：审核通过后没有生成费用账单
   - 影响：财务数据不完整
   - 修复：集成账单系统

#### 轻微问题（可以优化）

7. **前端租赁记录使用模拟数据**
   - 位置：`parking/rental/index.vue`
   - 问题：租赁记录不是真实数据
   - 影响：功能不完整
   - 修复：实现后端租赁合同管理

8. **缺少车位可视化**
   - 影响：用户不直观
   - 建议：增加车位平面图展示

9. **申请时车位选择不友好**
   - 问题：需要手动输入车位编号
   - 建议：提供下拉选择，只显示可用车位

10. **缺少费用自动计算**
    - 问题：需要手动计算总金额
    - 建议：前端自动计算并显示费用明细

### 7.2 开发优先级建议

#### P0 - 紧急（1-2天）
1. ✅ 修复审核人硬编码问题
2. ✅ 修复分页性能问题
3. ✅ 创建 Mapper XML 文件
4. ✅ 审核通过时更新车位状态
5. ✅ 添加权限注解

#### P1 - 重要（3-5天）
1. ✅ 创建租赁合同表
2. ✅ 实现租赁合同 Service
3. ✅ 审核通过时生成账单
4. ✅ 实现租赁记录真实数据
5. ✅ 添加事务管理

#### P2 - 需要（1周）
1. ✅ 实现定时任务（到期检查）
2. ✅ 集成通知系统
3. ✅ 优化前端车位选择
4. ✅ 添加费用自动计算
5. ✅ 实现合同终止功能

#### P3 - 优化（2周）
1. ✅ 车位可视化展示
2. ✅ 车位使用记录
3. ✅ 续租功能
4. ✅ 导出功能
5. ✅ 租赁统计报表

---

## 八、测试建议

### 8.1 单元测试

```java
@SpringBootTest
class ParkingRentalApplicationServiceTest {

    @Autowired
    private IParkingRentalApplicationService service;

    @Test
    void testReviewApplication() {
        // 测试审核通过
        boolean result = service.reviewApplication(1L, 2, "审核通过", 1L, "admin");
        assertTrue(result);

        // 验证车位状态已更新
        ParkingRentalApplication application = service.selectParkingRentalApplicationById(1L);
        assertEquals(2, application.getApplicationStatus());
    }

    @Test
    void testGetStatistics() {
        Map<String, Object> stats = parkingSpaceService.getParkingSpaceStats();

        assertNotNull(stats);
        assertTrue(stats.containsKey("totalCount"));
        assertTrue(stats.containsKey("usageRate"));
    }
}
```

### 8.2 集成测试

```java
@AutoConfigureMockMvc
@SpringBootTest
class ParkingControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    @WithMockUser(username = "admin")
    void testReviewApplication() throws Exception {
        mockMvc.perform(put("/api/v1/parking/rental/application/1/review")
                .param("status", "2")
                .param("reviewRemark", "审核通过"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.code").value(200));
    }
}
```

---

## 九、总结

### 9.1 模块现状

**已完成** ✅：
- 车位基础管理（CRUD）
- 租赁申请提交
- 租赁申请审核
- 车位统计数据
- 前端页面展示

**待完成** ⏳：
- 租赁合同管理
- 账单集成
- 定时任务
- 通知集成
- 车位可视化
- 租赁续租/终止

**存在问题** ⚠️：
- 审核人硬编码
- 分页性能问题
- 缺少 Mapper XML
- 缺少事务管理
- 缺少权限控制

### 9.2 代码质量评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | 60% | 基础功能完成，高级功能缺失 |
| 代码规范性 | 75% | 代码结构清晰，部分命名不规范 |
| 性能优化 | 65% | 存在分页性能问题 |
| 安全性 | 70% | 缺少权限注解，审核人硬编码 |
| 可维护性 | 80% | 分层清晰，易于扩展 |
| 用户体验 | 70% | 基础交互完成，缺少可视化 |

**总体评分**：**70分**（良好）

### 9.3 后续建议

1. **立即修复** P0 级别问题
2. **补充完善** 租赁合同管理功能
3. **系统整合** 账单、通知等模块
4. **性能优化** 数据库查询、分页等
5. **用户体验** 可视化、自动计算等

---

**报告编写者**：Claude AI
**报告日期**：2025-01-31
**模块版本**：v1.0（开发中）
