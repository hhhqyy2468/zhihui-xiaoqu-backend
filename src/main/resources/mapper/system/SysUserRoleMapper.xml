<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyu.system.mapper.SysUserRoleMapper">

    <resultMap type="com.hyu.system.domain.SysUserRole" id="SysUserRoleResult">
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
    </resultMap>

    <delete id="deleteUserRoleByUserId" parameterType="Long">
        delete from sys_user_role where user_id = #{userId}
    </delete>

    <delete id="deleteUserRoleByUserIds" parameterType="String">
        delete from sys_user_role where user_id in
        <foreach item="userId" collection="array" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <insert id="batchInsertUserRole" parameterType="java.util.List">
        insert into sys_user_role(user_id, role_id) values
        <foreach item="roleId" collection="roleIds" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <select id="listRoleIdsByUserId" parameterType="Long" resultType="Long">
        select role_id from sys_user_role where user_id = #{userId}
    </select>

</mapper>